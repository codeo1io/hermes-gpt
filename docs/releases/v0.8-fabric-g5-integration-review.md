# Hermes GPT v0.8 Fabric — G5 integration evidence packet

Status: implementation review packet for #36. This document does not constitute the required independent review.

## Baselines

- Frozen historical implementation evidence baseline: `4bb85d1eb7abf0974077b63c131013613793bb72`.
- G5 implementation starting point: `21f2138aab6df086d89f6af28ae1c3f636f33dc9` (G4-D / PR #44).
- G5 implementation PR: #45.
- Product invariant: remote work may scale where execution runs; trust, evidence admission, Work Contract validation, and final human approval remain coordinator/Owner responsibilities.

## Cross-slice composition strategy

G5 intentionally tests production seams rather than duplicating the G4 slice unit suites. The primary composition path is:

`execution.backend=auto` → G4-C capability-aware routing → managed Fabric dispatch → peer execution → durable coordinator reconciliation → evidence/artifact admission → existing Work Contract validation → read-only Flight Deck projection.

The G4-A/B/C/D focused suites remain in CI alongside the new G5 integration file so failure-mode coverage is preserved while the composition path is exercised end to end in-process.

## Required scenario map

| #36 scenario | Evidence owner | Expected invariant |
| --- | --- | --- |
| Explicit remote peer + explicit runner | `test_operator_fabric_adversarial.py::test_authorized_remote_write_happy_path_returns_admissible_run` plus G4-C explicit-dispatch tests | Managed node/runner are server-controlled and remote success becomes only admissible structured evidence. |
| `auto` placement across local/remote candidates | `test_operator_fabric_integration.py::test_auto_local_dispatch_composes_while_remote_candidate_is_available`; `test_operator_fabric_integration.py::test_auto_remote_dispatch_evidence_contract_and_flight_deck_compose`; router/G4-C auto-placement suites | Hard filters precede ranking; both local and remote winners compose through `AutoBackend` without changing Work Contract authority. |
| Mixed local/remote Swarm fan-out/fan-in | `test_operator_fabric_integration.py::test_mixed_local_remote_swarm_fan_out_fan_in_and_owner_approval_compose`; `test_operator_swarm.py::test_auto_stage_contract_delegates_agent_choice_without_losing_owner_profile` | Swarm preserves the stage owner as the authority profile while `assigned_agent=auto` delegates placement; fan-in remains blocked until both local and admitted remote completion are observed. |
| Remote write-capable stage preserves authority end to end | `test_operator_fabric_integration.py::test_write_retry_cannot_overlap_and_epoch_moves_only_after_cancel`; G4-C ownership tests | Write ownership is conflict-domain scoped, monotonic, and cannot overlap a prior active writer. |
| Remote read-only stage cannot gain write through runner/router options | `test_operator_fabric_integration.py::test_auto_read_only_runner_options_cannot_widen_remote_authority` | A workspace-write sandbox option cannot upgrade Work Contract authorization or create a write claim. |
| Timeout after successful submit recovers same identity | `test_operator_fabric_integration.py::test_timeout_after_accept_restart_reconciles_same_attempt_and_view`; G4-A adversarial timeout test | Ambiguous submit is reconciled against the exact attempt; no duplicate accept is issued. |
| Coordinator restart while remote stage is running | same G5 timeout/restart test plus G4-A restart tests | Durable coordinator journal recovers the same attempt and observes RUNNING without replacement dispatch. |
| Peer loss + later recovery | G4-A/G4-C adversarial recovery suites | Loss is ambiguous/blocked, never success; later observation reconciles the exact lineage. |
| Cancellation/retry race does not leave two write-capable attempts active | G5 write retry test plus G4-C stale-epoch tests | Retry is blocked while prior ownership is active; stale attempts cannot release a newer epoch. |
| Artifact return with valid hashes | `test_operator_fabric_integration.py::test_remote_artifact_hash_is_admitted_and_active_content_stays_metadata_only` | Coordinator rehashes admitted bytes and Flight Deck exposes metadata only. |
| Artifact mismatch/unsafe path/oversize rejection | `test_operator_fabric_g4c.py` artifact hash, logical-name, symlink, lineage, size, and snapshot regressions | Invalid artifacts never enter admission storage. |
| Missing evidence or peer self-report cannot produce SATISFIED | `test_operator_fabric_adversarial.py` evidence provenance/self-certification regressions; G5 Work Contract composition test | Only coordinator-admitted provenance may satisfy a declared Work Contract check. |
| Flight Deck exposes stale/ambiguous/blocked states accurately | G5 timeout/restart view assertions plus `test_operator_fabric_view.py` and `test_ui_fabric.py` | Presentation is observational and explicit about stale, ambiguous, blocked, evidence, artifact, and placement state. |
| Final approval remains human/Owner-gated | `test_operator_fabric_integration.py::test_mixed_local_remote_swarm_fan_out_fan_in_and_owner_approval_compose`; `test_operator_fabric_integration.py::test_remote_completion_cannot_self_satisfy_required_human_review`; existing Swarm approval tests | Remote operational completion cannot manufacture review/approval evidence; the mixed workflow reaches `awaiting_approval`, rejects stage dispatch and workspace-level approval, and completes only through Owner approval. |

## G5 composition defect found and resolved

The expanded mixed-Swarm scenario initially failed before routing with `FABRIC_ROUTING_INVALID`: Swarm generated `assigned_agent=<stage owner>` even when the stage explicitly selected `execution.backend=auto`. The Fabric router correctly rejects that ambiguous combination.

Resolution: `_stage_contract` now separates execution placement from orchestration authority. Auto stages use `assigned_agent=auto`; `assigned_profile`, allowed profiles, authorization, and stage ownership remain unchanged. No router guard or authority ceiling was weakened. The direct adapter regression and the full mixed local/remote fan-out/fan-in workflow both pin this behavior.

## Verification gates

The current G5 integration PR CI is expected to require all of the following on the stabilized exact head:

1. full repository pytest on Python 3.10, 3.11, and 3.12;
2. focused Fabric/runner/G5 integration regressions;
3. explicit v0.7 local-only compatibility gate for `test_operator_contract.py`, `test_operator_swarm.py`, and `test_operator_runners.py`;
4. Ruff across Fabric, integration, UI, and package-hygiene code;
5. web dependency install, Vitest, and production build;
6. Windows PowerShell documentation parsing;
7. `python -m build` and `twine check`;
8. package hygiene scan for private machine paths, private-network addresses, known machine identifiers, operational metrics, `.env` files, key/cert files, logs, and Python/test caches;
9. package artifact upload only after all prerequisite jobs pass.

Compatibility note: coordinator rows created before the `forbidden_policy_sha256` migration intentionally remain fail-closed. An in-flight pre-migration remote attempt without pinned forbidden-policy lineage validates as `UNVERIFIED` and must be retried rather than being treated as satisfied.

## Source-of-truth audit

G5 primarily adds integration/review infrastructure and hardens existing Fabric seams. Independent-review fixes remain within the accepted G4 authority model. This branch additionally fixes one composition defect in the Swarm-to-Work-Contract adapter: stages using `execution.backend=auto` now emit `assigned_agent=auto` while retaining the stage owner as `assigned_profile` and allowed profile. The router safety invariant is unchanged; Swarm no longer silently pins an auto-routed stage to its orchestration owner.

Runtime source-of-truth remains:

- managed dispatch/evidence and coordinator journal: `operator_fabric.py`;
- G4-C write ownership, execution-unit reconciliation, artifact snapshots/admission, and retry lineage: `operator_fabric_g4c.py`, `fabric_write_guard.py`, `fabric_artifacts.py`;
- capability-aware `auto` placement and routing receipts: `operator_fabric_router.py`;
- Work Contract observed-state verdicts: `operator_contract.py`;
- read-only operator visibility: `operator_fabric_view.py`, `ui_fabric.py`;
- browser mutation authority: existing `ui_ops.py` gated path, unchanged by Fabric Flight Deck work.

Architecture source-of-truth remains the accepted v0.8 ADR/security package under `docs/design/` and `docs/releases/`; public release/operator documentation should be updated only in the release-publication gate, not by inventing pre-release behavior in current v0.7 docs.

## Independent review boundary

G5 is not complete until a reviewer independent of the implementation owner examines the stabilized PR head for cross-slice failure modes. The implementation owner may prepare evidence, answer findings, and make fixes, but must not self-certify the independent-review requirement.

Any critical/high finding blocks G6 until resolved or explicitly accepted in the #36 record. A clean implementation CI result alone does not close #36.

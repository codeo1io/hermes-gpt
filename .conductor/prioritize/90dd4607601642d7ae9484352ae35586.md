# Prioritize — repository-maintenance cycle 1, run 6d49d1e3979f

Attempt 90dd4607601642d7ae9484352ae35586 · 2026-09-21 · HEAD c1785b22e51741ab0b3553091d000a01ae73463b
Inputs: assess (68113cb2, 9 findings), research (5d6f8b9e), roadmap ledger ROADMAP.md (rm-001..rm-025; sibling interlock recorded in-file).

## Constraints that bound the selection

1. **Do-not-reimplement interlock**: rm-003 (PKCE test re-bind), rm-004 (audit-log home/rotation), rm-007 (mcp_compat skip guard), rm-009 (mcp CI lanes + protocol-revision assertion) are already implemented on the unmerged sibling branch `conductor/run-a51c0f6cb5b0` (commits 2ff57351b7, ff5ec0744f, 3e8eebee48; diverged from this run at 54f826c0cd). This cycle must not touch those surfaces — adopt-by-content at the merge gate instead.
2. **Local-verifiable only**: running CI is prohibited this cycle, so batch items must prove out with local commands (ruff, pytest, pip install). CI-workfile edits are allowed but count as unverifiable-here; only one such edit is in the batch (widening the lint file list) and it is trivially reviewable.
3. **One-turn implementable**: batch must complete end-to-end in a single implement phase without design notes or product decisions.
4. **Baseline honesty**: HEAD has 5 pre-existing pytest failures (4 stale PKCE tests + test_mcp_compat.py:81) — the batch must not regress beyond that baseline; those 5 are the sibling branch's scope, not ours.

## Scoring (1–5; effort XS<S<M<L; "gate-drift" = widens permanent regression protection)

| id | item | impact | risk-if-skipped | effort | deps | strategic | locally verifiable |
|----|------|--------|-----------------|--------|------|-----------|--------------------|
| rm-016 | delete dead token_store.py:1097-1145 (F821×5) | 3 | 2 | XS | none | 3 (enabler) | yes (ruff+pytest) |
| rm-017 | ruff 61→0 repo-wide + widen CI lint gate | 4 | 4 | M | rm-016 | 4 (gate-drift) | yes (ruff+pytest) |
| rm-023 | bound uvicorn (pyproject.toml:18, unbounded 0.x) | 4 | 4 (any install can break) | XS | none | 3 | yes (pip install+tests) |
| rm-018 | raise on empty code_challenge at issuance | 3 | 2 | S | none (compatible w/ rm-003) | 3 | yes (pytest) |
| rm-021 | backup+warn before corrupt jobs.json overwrite | 2 | 2 | S | none | 2 | yes (pytest) |
| rm-024 | py3.13/3.14 CI lanes + 3.10 EOL plan | 4 | 3 | M | green master CI | 4 | **no** (needs CI run) |
| rm-008 | master branch protection | 4 | 4 | S | merge-gate/admin | 4 | no (gh admin) |
| rm-010 | flaky rotation/reconcile root-cause | 3 | 3 | M-L | repeated CI runs | 2 | no |
| rm-005 | session-jobs retention + watcher finally | 3 | 2 | M | none | 2 | yes |
| rm-006 | integer/Decimal budget ledger | 3 | 2 | M-H (migration) | none | 3 | yes |
| rm-015 | configurable audit retention | 2 | 2 | M | rm-004 merge | 2 | yes |
| rm-011 | MCP tasks/get read-only mapping | 3 | 1 | L | SDK probe/design | 4 | partial |
| rm-012 | MRTR approval handshake | 3 | 1 | L | design note | 3 | partial |
| rm-013 | split server.py registration | 2 | 1 | L | quiet merge window | 2 | yes |
| rm-025 | D3 hard-block + controller live | 3 | 2 | L | product call | 3 | yes |
| rm-019/020/022 | lease semantics / resume edges / topo dedupe | 1–2 | 1–2 | S each | none | 1–2 | yes |
| rm-001/002 | coverage/complexity programs | 3 | 2 | standing | — | 3 | ongoing |
| rm-014 | subscriptions/listen study | 1 | 1 | S | push consumer | 1 | deferred by ledger |

Key facts behind the scores (re-derived this turn):
- `python3 -m ruff check .` = 61 errors; 30 auto-fixable; F401 breakdown: **0 in `__init__.py`** (no re-export hazards), 30 in ordinary modules. Errors live in token_store.py (7), server.py, operator_cron.py, oauth_auth.py, operator_contract.py, tests… — **none** in the CI lint job's file list (verified: that list passes clean).
- CI lint job (ci.yml:113-137) runs `ruff check <hand-maintained 25-file list>` — a gate over a shrinking subset; every new core file since is ungated. Widening to repo-wide after reaching zero removes the drift hazard permanently.
- `uvicorn` sits bare in pyproject.toml:18 while every other runtime dep is bounded — the single unbounded 0.x runtime dependency (PyPI latest 0.53.0).

## SELECTED BATCH — "lint zero + fail-loud + pin" (theme: permanent regression surface, no sibling collisions)

| order | item | why in batch |
|-------|------|--------------|
| 1 | **rm-016** delete dead token_store.py:1097-1145 | XS effort; removes the only F821 class (5/61 errors); prerequisite for a repo-wide green ruff |
| 2 | **rm-017** ruff 61→0 + widen lint gate to `.` | Highest value: converts a partial hand-maintained gate into whole-repo protection; effort bounded (30 autofix + 26 mechanical manual) |
| 3 | **rm-023** bound uvicorn `>=0.30,<1`-style cap in pyproject | XS effort, closes the only unbounded 0.x runtime dep; any pip install today can pick a breaking ASGI change |
| 4 | **rm-018** `issue_authorization_code` raises on empty/invalid code_challenge + unit test | S effort; turns mint-dead-credential confusion into a loud error; same fail-closed philosophy the code already asserts at exchange; no collision with sibling rm-003 (tests mint real S256 pairs there) |

Stretch (only if implement turn has margin): **rm-021** corrupt jobs.json backup+warn.

Rationale, compressed:
- All four are independent of the sibling branch's implemented surfaces → merge-safe, zero duplicated work.
- All four verify locally: `python3 -m ruff check .` → exit 0; full pytest no worse than the 5-failure HEAD baseline (token_store/oauth/cron suites specifically green); `pip install` resolves inside the new uvicorn bound.
- Compounding story: rm-016 feeds rm-017; rm-017's gate widening protects rm-018/rm-023's own code forever after.
- Deliberately **excluded**: rm-024/rm-010 (need CI runs to prove), rm-008 (admin/merge-gate action), rm-005/006/015 (medium, next cycle), rm-011/012/013/025 (need design/product decisions or a quiet merge window), rm-019/020/022 (low value), rm-014 (ledger-deferred), rm-003/004/007/009 (sibling-implemented, adopt at merge).

## Hand-off to implement

Verification gates for the batch: (a) `python3 -m ruff check .` exits 0; (b) `python3 -m pytest -q` fails with exactly the 5 known pre-existing failures, all five confined to the sibling-scoped tests (test_codex_pr63_remediation.py, test_mcp_compat.py:81); (c) oauth empty-challenge unit test passes and every existing oauth test stays green; (d) `python3 -m pip install -e ".[dev]"` still resolves with the uvicorn bound. Files expected to change: token_store.py, ~20 modules/tests for lint, ci.yml (lint job file list → `.`), pyproject.toml, oauth_auth.py + one test file.
